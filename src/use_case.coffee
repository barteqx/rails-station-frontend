class UseCase
  constructor: ->
    
  start: =>
    @askForStream()

  askForStream: =>

  streamProvided: (stream) =>
    @showStream(stream)

  showStream: (stream) =>

  restart: =>
    @askForStream()